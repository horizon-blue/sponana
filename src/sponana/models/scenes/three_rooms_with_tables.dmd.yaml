directives:
  # Table tops
  - add_model:
      name: table_top0
      file: package://sponana/table_top.sdf

  - add_weld:
      parent: world
      child: table_top0::table_top_center
      X_PC:
        translation: [0, -4.0, 0.2]

  - add_model:
      name: table_top1
      file: package://sponana/table_top.sdf

  - add_weld:
      parent: world
      child: table_top1::table_top_center
      X_PC:
        translation: [0, 0, 0.2]

  - add_model:
      name: table_top2
      file: package://sponana/table_top.sdf

  - add_weld:
      parent: world
      child: table_top2::table_top_center
      X_PC:
        translation: [0, 4.0, 0.2]

  # Walls and floor
  - add_model:
      name: side_wall_0
      file: package://sponana/side_wall.sdf

  - add_weld:
      parent: world
      child: side_wall_0::side_wall_center
      X_PC:
        translation: [-2.0, -6.0, 0.0]

  - add_model:
      name: side_wall_1
      file: package://sponana/side_wall.sdf

  - add_weld:
      parent: world
      child: side_wall_1::side_wall_center
      X_PC:
        translation: [-2.0, -2.0, 0.0]

  - add_model:
      name: side_wall_2
      file: package://sponana/side_wall.sdf

  - add_weld:
      parent: world
      child: side_wall_2::side_wall_center
      X_PC:
        translation: [-2.0, 2.0, 0.0]

  - add_model:
      name: side_wall_3
      file: package://sponana/side_wall.sdf

  - add_weld:
      parent: world
      child: side_wall_3::side_wall_center
      X_PC:
        translation: [-2.0, 6.0, 0.0]

  - add_model:
      name: floor
      file: package://sponana/platform.sdf

  - add_weld:
      parent: world
      child: floor::platform_center
      X_PC:
        translation: [1.0, 0, 0]

  - add_model:
      name: back_wall
      file: package://sponana/back_wall.sdf

  - add_weld:
      parent: world
      child: back_wall::back_wall_center
      X_PC:
        translation: [-2.0, 0.0, 0.0]

  - add_model:
      name: front_wall_0
      file: package://sponana/front_wall.sdf

  - add_weld:
      parent: world
      child: front_wall_0::front_wall_center
      X_PC:
        translation: [2.0, -4.0, 0.0]

  - add_model:
      name: front_wall_1
      file: package://sponana/front_wall.sdf

  - add_weld:
      parent: world
      child: front_wall_1::front_wall_center
      X_PC:
        translation: [2.0, 0.0, 0.0]

  - add_model:
      name: front_wall_2
      file: package://sponana/front_wall.sdf

  - add_weld:
      parent: world
      child: front_wall_2::front_wall_center
      X_PC:
        translation: [2.0, 4.0, 0.0]

  - add_model:
      name: starting_area
      file: package://sponana/starting_area.sdf

  - add_weld:
      parent: world
      child: starting_area::starting_area_center
      X_PC:
        translation: [3.0, 7.0, 0]
